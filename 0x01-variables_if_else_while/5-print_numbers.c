#include <stdio.h>

/**
 * main - main function
 *
 * prints base 10 numbers starting
 * with 0 followed by a newline
 *
 * Return: always 0
 */

int main(void)
{
	int a;

	for (a = 0; a < 10; a++)
		printf("%d", a);
	printf("\n");

		return (0);
}
