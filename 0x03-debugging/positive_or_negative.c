#include "main.h"

/**
 * positive_or_negative - main function
 *
 * Return: 0
 */

positive_or_negative()
{
	if (n>0)
		putchar("%d\n, is a positive number");
	else if (n = 0)
		putchar("%d\n, is zero);
	else
		putchar
